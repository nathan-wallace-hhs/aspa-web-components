// .storybook/preview.js

// Configure Storybook's preview settings here

import { addParameters } from '@storybook/react';

addParameters({
  options: {
    showPanel: true,
    panelPosition: 'bottom',
  },
});
